# SkillRadar 用户手册（v0.5）

> AI 基础插件商业情报与市场调研 **桌面应用** · 项目代号 **SkillRadar** · 版本 **0.5.0**  
> 更新：2026-08-31

SkillRadar 是安装在本机的 **Electron 窗口**：自动拉起本地 API，按关键词列表定时扫描 GitHub 上的 AI Skill 仓库，把命中写入仓库库与图存储，并在雷达上点亮。随后可拆解结构、生成架构图、逆向设计动机，并产出 PRD。用户可按关键词 / 作者 / 仓库订阅情报，通过**飞书**或**企业微信**机器人按周期接收简报。

---

## 1. 你能用它做什么

| 模块 | v0.5 能力 |
|------|-----------|
| 桌面应用 | Electron 窗口、菜单/托盘、单实例、外链走系统浏览器；双击一键启动 |
| 关键词监控 | 维护关键词 / Topic / 作者列表，按间隔自动扫 GitHub；可立即跑、暂停、删除 |
| 情报雷达 | 指纹分环（Claude Skill / MCP / LangChain），星标变化、命中词、同类连线；点星预览再拆解 |
| 图存储 | 配置 `NEO4J_URI` 后写入 Neo4j（`HIT` / `RELATED_TO` / `CONTAINS`）；未配置则内存图 |
| 仓库拆解 | 浅克隆或本地路径解析，提取技能、工具、依赖 |
| 可视化架构 | 模块依赖 / 调用链 / 数据流，前端 React Flow 交互 |
| 设计动机 | README + 结构启发式分析；配置 `LLM_API_KEY` 后走模型 |
| PRD | 自动填充 Markdown 模板，可在线编辑与导出 |
| 情报订阅 | 日/周/月简报，飞书卡片或企微 Markdown 推送 |
| 市场调研 | PEST / 政策 / ≥5 竞品 / 痛点论证 / MVP，证据可溯源 |
| 商业拆解 | 含第 5 章需求调研详细分析，数据来自市场调研 JSON |
| 连接配置 | 大模型（OpenAI/Anthropic/本地）与飞书/企微/邮件渠道，AES-256-GCM |

未配置 Neo4j 时雷达与拆解图仍可用（内存 / Postgres JSON）。默认识别进程内定时器。Qdrant / MinIO 仍为后续版本预留。

---

## 2. 安装与启动

本机需要：**Python 3.11+**、**Node.js 18+**（或 nvm）。可选 GitHub Token 以提高搜索限额。

### 2.1 双击启动 Electron（推荐）

```bash
cd skillradar
chmod +x 启动SkillRadar.sh
./scripts/install-desktop.sh
./启动SkillRadar.sh
```

或在文件管理器中双击 `启动SkillRadar.sh` / 桌面上的 **SkillRadar 情报雷达**。

脚本会：创建 `.venv` → 安装后端依赖 → 安装 Electron → **打开桌面窗口**。窗口里先出现启动动画，待 API 与页面就绪后进入首页。

- 菜单 **转到**：雷达（Ctrl+1）、订阅（Ctrl+2）、设置（Ctrl+3）
- 菜单 **帮助**：应用内手册、打开 `用户手册.md`、Swagger API 文档
- 托盘图标：打开窗口 / 退出
- 第二次双击不会再开一个进程，而是把已有窗口前置

开发数据默认写在项目 `data/`；若使用打包后的 AppImage，数据在用户目录（设置页可查看路径）。

浏览器模式（不推荐）：`SKILLRADAR_BROWSER=1 ./启动SkillRadar.sh`

### 2.2 打包 AppImage

```bash
./scripts/build-electron.sh
# 产出 frontend/release/SkillRadar-0.5.0-linux.AppImage
```

运行 AppImage 仍需要本机 Python（用于 uvicorn）。首次请用 `启动SkillRadar.sh` 装好 `.venv`，或设置 `SKILLRADAR_PYTHON` 指向已安装依赖的解释器。

### 2.3 开发者手动启动

终端一：

```bash
cd skillradar
python3 -m venv .venv && source .venv/bin/activate
pip install -r backend/requirements.txt
export PYTHONPATH=$PWD/backend APP_VERSION=0.5.0
cd backend && uvicorn app.main:app --reload --port 8000
```

终端二：

```bash
cd skillradar/frontend && npm install && npm run electron:dev
```

`electron:dev` 会在 API 已存在时直接加载窗口；否则由 Electron 拉起 uvicorn 与 `next dev`。

健康检查：http://127.0.0.1:8000/api/v1/health 应返回 `"status":"ok","version":"0.5.0"`。

### 2.4 Docker Compose（完整图数据库）

复制 `.env.example` 为 `.env`，至少改 `SECRET_KEY`。可选 `GITHUB_TOKENS`、`LLM_API_KEY`、`NEO4J_PASSWORD`。

```bash
docker compose up --build
```

前端 :3000，后端 :8000，Neo4j :7474。Electron 同样可以连这套已启动的本地端口（窗口发现 8000/3000 已健康就不会重复拉起进程）。

### 2.5 部署到极空间 NAS

NAS 侧仍是浏览器访问的轻量 Docker 栈（SQLite + 内存图），与本机 Electron 互补。

```bash
cp .nas-deploy.env.example .nas-deploy.env
./scripts/AutoDeployToNAS.sh
```

- 前端：`http://<NAS_IP>:13000/`
- API：`http://<NAS_IP>:18000/api/v1/health`

---

## 3. 使用流程

1. 打开桌面窗口 → **登录 / 注册**（密码至少 8 位）。  
2. **雷达**：首次已有种子监控词（`mcp server`、`claude skill`、`langchain`、作者 `anthropics`）。  
   - 输入关键词 → **扫描 GitHub** 或 **扫描并监控**。  
   - 左侧可暂停 / 立即扫描 / 删除；**跑到期监控** 把到期词排队。  
   - 点星出现预览卡，再「进入拆解」或用系统浏览器打开 GitHub。  
3. 仓库页：尚未拆解时点「从雷达拆解此仓」→ 架构图 →「生成动机分析」→ **PRD 工作台**。  
4. **订阅**：关键词、频率、飞书/企微 Webhook →「发送测试简报」。  

没有 GitHub Token 时搜索容易被匿名限额拦住；拆解公开仓库仍可用。设置页可看图存储后端以及桌面数据目录。

---

## 4. 飞书 / 企业微信

### 飞书自定义机器人

1. 群设置 → 添加机器人 → 自定义机器人，复制 Webhook。  
2. 可选开启签名校验，把 secret 填进订阅。  
3. SkillRadar 发送交互卡片（标题 + Markdown）。  

### 企业微信群机器人

Webhook 形如 `https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=...`。Markdown 超过 4096 字节会截断。

Webhook 与 secret 使用 **AES-256-GCM** 加密入库，接口不回传明文。

---

## 5. API 摘要（JWT，除注册/登录/健康检查）

Base：`/api/v1`  
响应：`{ "code": 0, "data": {}, "message": "success" }`  
Header：`Authorization: Bearer <token>`

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/auth/register` `/auth/login` | 注册 / 登录 |
| GET | `/radar` | 雷达快照 |
| POST | `/scan/github` | `{query, type, limit, watch?}` |
| GET | `/scan/tasks` `/scan/tasks/{id}` | 任务 |
| CRUD | `/scan/keywords` | 监控词 |
| POST | `/scan/keywords/{id}/run` | 立即扫描 |
| POST | `/scan/keywords/run-due` | 到期监控 |
| GET | `/repos` | 仓库列表 |
| POST | `/repos/decompose` | `{repo_url, local_path?}` |
| GET | `/repos/{id}/structure` `/graph` `/motivation` `/prd` | 拆解产物 |
| POST | `/repos/{id}/motivation` | 动机 + PRD |
| CRUD | `/subscriptions` | 订阅 |

扫描为后台任务：先拿 `task_id`，再看任务状态；成功后刷新雷达。同一仓库再次扫描会更新 `star_delta`。

---

## 6. 环境变量

见 `.env.example`。关键项：

- `SECRET_KEY`：JWT 与缺省加密盐  
- `DATABASE_URL`：桌面默认 SQLite；Compose 可用 PostgreSQL  
- `GITHUB_TOKENS`：逗号分隔，搜索轮换  
- `NEO4J_URI`：为空则内存图  
- `LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL`  
- `ENCRYPTION_KEY`：Webhook 加密  
- `SKILLRADAR_PYTHON`：Electron 使用的 Python 路径  

Electron 会把 `APP_VERSION=0.5.0`、`CORS_ORIGINS=http://127.0.0.1:3000` 注入子进程。

---

## 7. 定时任务

进程内 APScheduler：

| 任务 | 周期 |
|------|------|
| 到期关键词 GitHub 扫描 | 每小时检查 `interval_hours` |
| 仓库统计 | 每天 02:00 UTC |
| 到期订阅简报 | 每小时 |
| 清理临时克隆 | 每天 03:00 UTC |

`SKILLRADAR_DISABLE_SCHEDULER=1` 可在测试中关闭。首次启动写入种子监控词。

---

## 8. 安全约定

- 密码 bcrypt；JWT HS256。  
- 默认每 IP 每分钟 60 次（健康检查除外）。  
- 仅允许 clone `github.com` / `gitlab.com` / `gitee.com`。  
- 只分析公开仓库；遵守原仓库许可证。  
- 不要把 Token、Webhook、LLM Key、Neo4j 密码、`.nas-deploy.env` 提交到 Git。  
- 桌面应用只监听 `127.0.0.1`，不把 API 暴露到局域网（除非你改 host）。  

---

## 9. 故障排除

| 现象 | 处理 |
|------|------|
| 窗口停在「正在启动」 | 看 `data/electron-child.log`（AppImage 则在用户数据目录）；确认 Python 能 `import uvicorn` |
| 提示端口被占用 | 已有实例在跑：从托盘打开窗口，或结束旧进程后重启 |
| GitHub 扫描失败 | 填写 `GITHUB_TOKENS`；匿名限额约每小时数十次 |
| 拆解失败 | 仓库须公开；本机已装 `git` |
| 想用浏览器 | `SKILLRADAR_BROWSER=1 ./启动SkillRadar.sh` |

---

## 10. 校验清单（v0.5）

- [x] Electron 窗口加载雷达，菜单/托盘可用  
- [x] `GET /api/v1/health` → 200，`version: 0.5.0`  
- [x] 监控词 CRUD / 扫描结果进雷达  
- [x] 本地 fixture 可拆解 Claude Skill  
- [x] `./test.sh`：pytest 覆盖率门禁 + 健康检查 + 雷达冒烟  

```bash
chmod +x test.sh && ./test.sh
```

---

## 11. 版本记录

| 版本 | 说明 |
|------|------|
| **0.5.0** | 市场调研、商业拆解需求调研章、LLM/渠道配置中心、移动优先界面 |
| **0.3.0** | Electron 桌面应用；菜单/托盘/单实例；双击启动进窗口；AppImage 打包脚本；应用内手册 |
| **0.2.0** | Neo4j/内存图；关键词定时扫描；雷达交互；一键脚本；AutoDeployToNAS |
| **0.1.0** | 初版：扫描、拆解、图谱、动机/PRD、飞书/企微订阅 |


---

## 12. 市场调研

在插件（仓库）详情页打开 **市场调研** Tab，点击生成。报告包含：

1. 市场定义与规模  
2. 宏观环境（PEST）  
3. 产业政策、五力与 SWOT  
4. 至少 5 个竞品对比  
5. 3–5 个痛点及证据  
6. MVP 建议（P0/P1、验证指标）

每个结论附 URL 或推理依据。可人工编辑 Markdown 后保存。未配置大模型时使用启发式分析（测试环境可离线通过）。

API：

- `POST /api/v1/plugins/{id}/market-research`  
- `GET /api/v1/repos/{id}/market-research`  
- `PUT /api/v1/market-research/{report_id}`

## 13. 系统设置（大模型与渠道）

路由 `/settings`：

- **大模型**：提供商、API Key、模型、温度；「测试连接」发送 ping。  
- **渠道**：飞书 Webhook / 企微 Webhook / 邮件 SMTP，均可测试发送。  
- 订阅可填写 `llm_config_id` 与 `channel_config_id`，实现多配置集隔离。

密钥仅加密存储，列表接口只返回掩码。环境变量 `ENCRYPTION_KEY` 用于 AES-256-GCM。
