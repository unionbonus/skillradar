# SkillRadar 用户手册（v0.5.2）

> AI 基础插件 **商业情报与市场调研** 桌面应用 · 项目代号 **SkillRadar** · 版本 **0.5.2**  
> 更新：2026-09-04

SkillRadar 持续扫描 GitHub、npm、PyPI、MCP Registry、Hugging Face、Docker Hub 等渠道上的 AI Skill / MCP Server / CLI 工具，完成结构拆解、技术深度分析、设计亮点挖掘和市场调研，并生成咨询级 **商业拆解报告**。报告进入报告库，支持全文与语义检索；订阅简报可推送到飞书、企业微信或邮件。

---

## 1. 你能用它做什么

| 模块 | v0.5.2 能力 |
|------|-----------|
| 桌面应用 | Electron 窗口、底部移动导航 / 顶部桌面导航、菜单/托盘、单实例 |
| 军事 PPI 雷达 | 同心距标、方位刻度、扫描余辉、IFF 接触点；热度越靠近中心 |
| 多渠道扫描 | GitHub / npm / PyPI / MCP Registry / Hugging Face / Docker Hub |
| 飞书 / 企微 | 未连接显示绑定二维码；连接成功显示客户端头像 + 右下角绿钩；SSE/WS 默认长连接 |
| 图存储 | `NEO4J_URI` → Neo4j；未配置则内存图 |
| 向量检索 | `QDRANT_URL` → Qdrant；未配置则哈希向量内存库 |
| 对象存储 | MinIO；未配置则本地 `data/objects/`（PRD / 报告 Markdown） |
| 插件拆解 | 浅克隆或本地路径，提取技能、工具、依赖、文件树 |
| 技术深度分析 | 架构风格、核心模块、调用链 Mermaid、扩展点、数据流 |
| 设计亮点 | 对比默认做法，附证据路径 |
| 市场调研 | PEST、政策、≥5 竞品、痛点论证、MVP 建议 |
| 商业报告 | 九章咨询体例，可编辑，进报告库 |
| 情报订阅 | 日/周/月；飞书卡片 / 企微 Markdown / SMTP 邮件 |
| 配置中心 | 大模型、飞书/企微扫码绑定、邮件渠道；密钥加密。配置与试用前需登录，前端会把登录令牌随 Cookie / `X-SkillRadar-Token` 转发给 API |

未配置 Neo4j / Qdrant / MinIO 时，雷达、检索与报告仍可用（内存图 + 哈希向量 + 本地对象）。这是为了单机和 NAS 轻量栈能过检。

---

## 2. 安装与启动

本机需要：**Python 3.11+**、**Node.js 18+**（或 nvm）。可选 GitHub Token。

### 2.1 双击启动 Electron（推荐）

```bash
cd skillradar
chmod +x 启动SkillRadar.sh
./scripts/install-desktop.sh
./启动SkillRadar.sh
```

脚本会：创建 `.venv` → 安装后端依赖 → 安装 Electron → **打开桌面窗口**。

浏览器模式：`SKILLRADAR_BROWSER=1 ./启动SkillRadar.sh`

### 2.2 开发者手动启动

```bash
python3 -m venv .venv && source .venv/bin/activate
pip install -r backend/requirements.txt
export PYTHONPATH=$PWD/backend APP_VERSION=0.5.2
cd backend && uvicorn app.main:app --reload --port 8000
```

另一终端：`cd frontend && npm install && npm run electron:dev`

健康检查：http://127.0.0.1:8000/api/v1/health 应返回 `"version":"0.5.2"`，并包含 `graph` / `vector` / `objects`。

### 2.3 Docker Compose（完整栈）

```bash
cp .env.example .env   # 至少改 SECRET_KEY
docker compose up --build
```

前端 :3000，后端 :8000，Neo4j :7474，Qdrant :6333，MinIO :9000。

### 2.4 部署到极空间 NAS

```bash
cp .nas-deploy.env.example .nas-deploy.env
./scripts/AutoDeployToNAS.sh
```

- 前端：`http://<NAS_IP>:13000/`
- API：`http://<NAS_IP>:18000/api/v1/health`

NAS 栈为 SQLite + 内存图 + 本地对象，适配内存限制。部署脚本会停掉占用 13000 的旧版 `skillradar-go`（v0.4），改由 v0.5 Next 前端接管该端口。不会改动星觅（`knowledge-graph-go`）目录。

---

## 3. 使用流程

1. **登录 / 注册**（密码至少 8 位）。  
2. **雷达**：选择渠道与关键词 → 扫描。PPI 扫描线上的接触点进入插件详情。  
3. **插件详情** Tab：概览 / 架构图 / 深度分析 / 动机 / 市场调研 / 报告。  
4. **生成报告** 后到 **报告库** 检索与编辑。  
5. **设置 → 渠道**：以 **飞书 / 企业微信 / 邮件** 标签页切换。飞书与企微未连接时显示二维码，连接成功后显示客户端头像且右下角绿钩，默认长连接。邮件渠道填 SMTP 收件人。配置大模型或渠道前需先登录；请求会同时携带 Authorization、X-SkillRadar-Token 与 Cookie，避免代理丢掉 Bearer。  
6. **订阅**：关键词 + 已绑定渠道。  

没有 GitHub Token 时匿名搜索易被限额；拆解公开仓库仍可用。

---

## 4. API 摘要（JWT，除注册/登录/健康检查）

Base：`/api/v1`  
响应：`{ "code": 0, "data": {}, "message": "success" }`

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/scan` 或 `/scan/github` | `{query, source, type, limit, watch?}` |
| POST | `/plugins/decompose` | 拆解 |
| GET/POST | `/plugins/{id}/deep-dive` `/motivation` `/market-research` | 分析 |
| POST | `/reports/generate` | `{plugin_id}` |
| GET | `/reports` `/reports/search` `/reports/{id}` | 报告库 |
| GET | `/search?q=` | 语义 + 全文 |
| CRUD | `/configs/llm` `/configs/channels` | 配置中心 + `/test` |
| GET | `/channels/live/status` | 飞书/企微二维码或在线状态 |
| POST | `/channels/live/{feishu\|wecom}/confirm` | 本机确认绑定 |
| GET/WS | `/channels/live/stream` `/channels/live` | SSE / WebSocket 长连接（`?token=`） |
| GET/POST | `/channels/bind/{ticket}` | 扫码确认页 |
| CRUD | `/subscriptions` | 订阅；`/test-send` 仍可用 `/send-test` |

---

## 5. 环境变量

见 `.env.example`。新增：

- `QDRANT_URL`：为空则内存向量  
- `MINIO_ENDPOINT` / `MINIO_ACCESS_KEY` / `MINIO_SECRET_KEY`：为空则本地对象  
- `SMTP_HOST` 等：邮件渠道  

密钥、Webhook、LLM Key 不要提交 Git。

---

## 6. 校验清单（v0.5.2）

- [x] `GET /api/v1/health` → `version: 0.5.2`，含 vector / objects  
- [x] 军事 PPI 雷达（扫描余辉、方位/距离、IFF）  
- [x] 飞书/企微未连接二维码，连接后头像绿钩，默认长连接  
- [x] 多渠道扫描适配器（未配置外网时 GitHub 主路径仍可用）  
- [x] 深度分析 / 市场调研 / 商业报告  
- [x] 语义检索与报告库  
- [x] 大模型与渠道配置中心  
- [x] 邮件渠道（需 SMTP）  
- [x] `./test.sh` 单测覆盖率门禁 + 健康检查  

```bash
chmod +x test.sh && ./test.sh
```

---

## 7. 版本记录

| 版本 | 说明 |
|------|------|
| **0.5.2** | 军事 PPI 雷达；飞书/企微/邮件渠道标签页；扫码绑定、头像绿钩、SSE/WS 长连接；代理友好鉴权 |
| **0.5.0** | 多渠道扫描；深度分析/亮点/市场调研；商业报告库；Qdrant/MinIO 可选；配置中心；邮件推送；移动优先 UI |
| **0.3.0** | Electron 桌面应用；菜单/托盘/单实例；双击启动；AppImage |
| **0.2.0** | Neo4j/内存图；关键词定时扫描；雷达交互 |
| **0.1.0** | 扫描、拆解、图谱、动机/PRD、飞书/企微订阅 |
